
doctype html
html
    head
        block header
            title storyteam
    body
        block content
            p storyteam will be back shortly
            
        script(src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js")
        script(src="https://cdn.socket.io/socket.io-1.2.0.js")
        script.

            if (document.loadedScripts === undefined) {
                document.loadedScripts = {};
            }
            if (document.loadedScripts['layout.jade'] !== true){
                document.loadedScripts['layout.jade'] = true;

                document.inWindow = true;
                $(window).blur(function(){
                    document.inWindow = false;
                }).focus(function(){
                    document.inWindow = true;
                });
            }



        block scripts

